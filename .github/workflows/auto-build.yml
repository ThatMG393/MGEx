name: Auto Build

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        target_arch: [
            arm64-v8a,
            armeabi-v7a
        ]

      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup JDK 17
        uses: actions/setup-java@v3.11.0
        with:
          distribution: adopt
          java-version: 17

      - name: Setup Android SDK & NDK
        uses: amyu/setup-android@v4
        with:
          sdk-version: 34
          cmake-version: 3.22.1
          ndk-version: 26.1.10909125
      
      - name: Build MGEx
        run: |
          mkdir b && cd b
          ls $ANDROID_SDK_ROOT
          ls $ANDROID_SDK_ROOT/ndk
          
          cmake .. -DCMAKE_BUILD_TYPE=Release -DANDROID_ABI=${{matrix.target_arch}} -DANDROID_PLATFORM=android-30 -DANDROID_STL=gnustl_static -DCMAKE_TOOLCHAIN_FILE="$ANDROID_SDK_ROOT/ndk/build/cmake/android.toolchain.cmake"
          make -j$(nproc)

      - name: Upload libMGEx.so
        uses: actions/upload-artifact@v3
        with:
          name: libMGEx-lib
          path: libMGEx.so



